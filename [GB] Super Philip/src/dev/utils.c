/* utilities file */


/* absolute value of a number
@param number: the number
@return: the absolute value of the number */
int abs(int number) {

	if (number < 0)
		return -number;
	else
		return number;
}